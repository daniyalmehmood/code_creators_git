public class sleep {
        public static boolean istSleepIn ( boolean weekday, boolean vacation)
        {
            if (!weekday  || vacation) {
                return true;
            
            } else {
                return false;
            }
        }
    }
