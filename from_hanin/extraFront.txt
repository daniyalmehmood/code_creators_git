public class FrontRepeater {
    public static String tripleFront(String str) {
        if (str.length()>=2) {
            return str.substring(0,2)+str.substring(0,2)+str.substring(0,2);

        }
        if (str.length()>=1) {
            return str+str+str;}
        return str;
    }

}