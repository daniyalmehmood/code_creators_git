public class StringSlicer {
    public String getTwoChar(String str, int index) {
        if (str.length() - 1 > index && str.length() < 2) {
            return str.substring(index, index + 2);
        
        }
        return str.substring(0, 2);
    }

}
