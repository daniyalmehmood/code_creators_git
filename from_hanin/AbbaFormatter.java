// Class that builds a string in ABBA format
public class AbbaFormatter {

    // Method that returns a string in the form: a + b + b + a
    public static String buildAbbaString(String a, String b) {
        return a + b + b + a;
    }
}
