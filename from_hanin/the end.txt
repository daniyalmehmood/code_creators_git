public class StringEndPicker {
    public String theEnd(String str, boolean front) {
        if (str != null && front) {
            return str.substring(0,1);}
        else if (str != null && !front) {
            return str.substring(str.length()-1);
        }

        return str;
    }

    }